<UserControl x:Class="TimeTracker.UI.Components.ucTimeRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeTracker.UI.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
   <Border BorderBrush="#cf5a07" BorderThickness="0,1,0,0" Padding="10">
      <Grid>
         <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
         </Grid.ColumnDefinitions>

         <Grid Grid.Column="0">
            <TextBox Text="{Binding description, FallbackValue=description}" VerticalAlignment="Center" FontSize="16" BorderThickness="0" />
         </Grid>

         <Grid Grid.Column="1">
            <StackPanel Orientation="Horizontal">
               <TextBlock Text="{Binding session_total_time, StringFormat=\{0:hh\\:mm\\:ss\}, FallbackValue=00:00:00}" VerticalAlignment="Center" Margin="15,0,15,0" FontSize="20" FontWeight="SemiBold" />
            </StackPanel>
         </Grid>
      </Grid>
   </Border>
</UserControl>
